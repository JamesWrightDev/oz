import React from "react";
import Layout from "../components/Layout";
import Hero from '../components/Hero';

const Home = () => {
  return (
    <Layout>
      <Hero
        text="Discover Melbourne"
      />
    </Layout>
  );
};

export default Home;
